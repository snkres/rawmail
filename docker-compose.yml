services:
  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
    depends_on:
      - web
      - api

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - API_URL=http://api:3001
      - NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL:-http://localhost:3001}
      - IS_SAAS=${IS_SAAS:-true}
      - APP_DOMAIN=${APP_DOMAIN:-rawmail.sh}
      - APP_NAME=${APP_NAME:-rawmail}
      - NEXT_PUBLIC_IS_SAAS=${IS_SAAS:-true}
      - NEXT_PUBLIC_APP_NAME=${APP_NAME:-rawmail}
      - NEXT_PUBLIC_APP_DOMAIN=${APP_DOMAIN:-rawmail.sh}
    depends_on:
      - api

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql://rawmail:rawmail@postgres:5432/rawmail
      - REDIS_URL=redis://redis:6379
      - POLAR_ACCESS_TOKEN=${POLAR_ACCESS_TOKEN:-}
      - POLAR_WEBHOOK_SECRET=${POLAR_WEBHOOK_SECRET:-}
      - POLAR_SERVER=${POLAR_SERVER:-sandbox}
      - POLAR_TEAMS_PRODUCT_ID=${POLAR_TEAMS_PRODUCT_ID:-}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - BETTER_AUTH_URL=${BETTER_AUTH_URL:-http://localhost:3001}
      - WEB_URL=${WEB_URL:-http://localhost:3000}
      - NODE_ENV=production
      - IS_SAAS=${IS_SAAS:-true}
      - APP_NAME=${APP_NAME:-rawmail}
      - APP_DOMAIN=${APP_DOMAIN:-rawmail.sh}
      - ALLOWED_EMAIL_DOMAIN=${ALLOWED_EMAIL_DOMAIN:-}
      - OIDC_CLIENT_ID=${OIDC_CLIENT_ID:-}
      - OIDC_CLIENT_SECRET=${OIDC_CLIENT_SECRET:-}
      - OIDC_ISSUER_URL=${OIDC_ISSUER_URL:-}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  mail:
    build:
      context: .
      dockerfile: apps/mail/Dockerfile
    ports:
      - "25:25"
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgresql://rawmail:rawmail@postgres:5432/rawmail
      - REDIS_URL=redis://redis:6379
      - APP_DOMAIN=${APP_DOMAIN:-rawmail.sh}
      - RAWMAIL_DOMAIN=${APP_DOMAIN:-rawmail.sh}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: rawmail
      POSTGRES_PASSWORD: rawmail
      POSTGRES_DB: rawmail
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rawmail"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
  caddy_data:
